### Role Model
This template uses a single-role-per-user model (`ROLE_USER`, `ROLE_ADMIN`) to keep authorization simple and explicit. The design can be extended to multi-role or permission-based systems if required.
